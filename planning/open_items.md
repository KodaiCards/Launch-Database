# Open Items — questions, pending decisions, parked recs, trigger-linked commitments (re-raise log)

> Owned by Planning. Nothing gets dropped; everything gets **re-raised at the right moment** — a stopping point, the relevant work area, a related question, or a named trigger. `commitments.md` MERGED in here (D019) — trigger-typed rows are the commitments. **CLOSED/DONE rows move to `planning/archive/`** (full pre-trim snapshot: `archive/open_items_pre-trim_2026-07-01.md`; git history has everything). Last updated 2026-07-01.

| # | Item | Type | Re-raise when | Status |
|---|---|---|---|---|
| O2 | **Feature deep-dive** (post-training roadmap; Job Board first) | parked thread | after the training launch-set ships | PARKED |
| O4 | **Billing submission samples** (RUS inspection / RUS design / non-RUS permitting) + the RUS code list | input from Carter | when billing (System A) starts | OPEN |
| O5 | **Does Workforce export CSV?** (+ a sample) | input from Carter | when hours (System D) starts | OPEN |
| O6 | **Map delivery timing** (external/collab source, D016 — integrate-as-delivered) | input from Carter | when the artifact lands | OPEN |
| O7 | **Cockpit alert thresholds** (fixed-fee %, RUS cap %, utilization) | input from Carter | when cockpit (System E) starts | OPEN |
| O8 | **~17 stale branches** — keep/delete each (`claude-2/*`, `claude-4/5/*`, `claude/*` randoms, `feature/*`, `orchestrator-*`, `worktree-*`, `redesign/*`) | hygiene | at a cleanup stopping point | OPEN |
| O9 | **Regression net largely EXISTS** (64 test files). CI is permanently dead (O43) so (a) is void → **`npm test` + a live/runtime pass MUST be run before every merge (Planning's gate — the only safety net now).** Planning has **verified local DB egress + query access** to prod; sandboxed workers (Auditor confirmed) can't reach the DB, so the **live/runtime verification at merge is Planning's, not a worker's** (workers do code + unit only). (b) still add 2 gap tests — keystone SA-delete cascade (O14) + legacy↔keystone hours reconciliation (O23) | recommendation | before merges / with the hours work | OPEN |
| O10 | **Staging-before-live** — `main` auto-deploys straight to prod; a Railway staging env is cheap insurance | recommendation | after the launch set; next infra gap now O11 backups are on | OPEN |
| O14 | **SA hard-delete = config-loss + hours-orphaning, no undo** (schema-verified: jobs/routes/materials CASCADE; hours SET NULL survive orphaned; billed SAs FK-RESTRICT-protected). Fix = wire the existing `saveUndoBucket`+`showUndoBar` | risk (medium) | before relying on SA delete; small fix | OPEN |
| O15 | **Canonical billing = `billing_keystone.js`** (progressive earned−billed ledger). Retire-set = legacy `billing.js` paths + `service_areas.js :id/bill` (orphan) — full table in `codebase/07-billing.md` | architecture (resolved rec) | billing rebuild executes it | OPEN |
| O16 | **Orphan invoices from `service_areas.js :id/bill`** — leaves BOTH the invoice header (no `service_area_id`/`engineering_contract_id`) AND line items (no `service_area_job_id`) unlinked → invisible to keystone reporting (Auditor-confirmed, worse than first thought). Fix = retire/delegate to `billing_keystone /run` + repoint `money_view` reporting at the keystone link columns | architecture (scoped) | at billing rebuild | OPEN |
| O17 | **THREE "service-area"-ish tables coexist** — `service_areas` (keystone), `concentrators` (legacy rollup), `ec_service_areas` (EC picker). Cutover must reconcile/retire the legacy two | architecture | cutover planning | OPEN |
| O18 | **PARALLEL legacy/keystone structures = the cutover's true scope.** SA×3, contracts×2 (naming collision incl. `construction_contracts`), dashboards×2, **billing = 6 invoice-creation paths** (Auditor-corrected: bill-multiple `billing.js:34` · generate-monthly-invoice **`projects.js:1411`** · bill-and-clone `project_billing.js:129` · `:id/bill` orphan `service_areas.js:904` · keystone `/run` `billing_keystone.js:112` · **the batch flow** `billing.js:343/481`), money reporting×2, **projections parity gap** (legacy `automation.js` sparklines/forecast/monthly-draft RICHER than keystone `projections.js` — must PORT, not reroute), tree×2. Needs ONE reconciliation map before cutover | architecture ⭐ | cutover planning (its foundation) | OPEN |
| O19 | Dead shadowed `GET /api/billing/report` in `billing.js` (keystone wins the mount; nothing calls the legacy one) | cleanup (low) | billing rebuild | OPEN |
| O20 | ⭐ **#1 BILLING-CUTOVER BLOCKER: invoice data assembly + the RUS PDF are legacy-`projects`-ONLY.** `invoice_generator.js` = 0 `service_area_job_id` refs / 22 legacy refs (Auditor-verified) → a keystone-billed invoice has NO submittable RUS PDF. The rebuild must PORT assembly to `service_area_jobs` BEFORE retiring legacy | architecture/risk | billing rebuild — first | OPEN |
| O21 | ⭐ **RUS DAILY FIELD PAPERWORK not built anywhere** (verified: desktop=shell, inspection.js=read rollup; the forms exist only as training content). THE RUS-exclusive deliverable, done off-platform today → idea I9; decision trigger = O44 | confirmed gap | O44 fires | OPEN |
| O22 | ⭐ **Hours rounding silent + inconsistent** — manual/CSV snap to 0.25 (`snapHoursToQuarter`, test-locked = intentional) but the TIMECLOCK stores raw 2-decimal (`timeclock_module.js:550,643`); editing a timeclock entry silently snaps it. **POLICY decision for Carter** (silent-snap vs reject-non-grid); changing it updates `hours_quarter_snap.test.js` | trust/policy | with the hours unification (O23/O45) | OPEN |
| O23 | ⭐⭐ **Hours SPLIT-BRAIN — the trust root-cause.** Legacy writers (manual, `hours_csv`, **the timeclock**) → `time_entries.project_id`; keystone writers → `service_area_job_id`; both columns exist, NO XOR CHECK (app-enforced only; `budgets.budget_scope_exactly_one` is the precedent CHECK to add post-unify); two disjoint view sets → different totals per screen. Unify on `service_area_job_id`, migrate, add the CHECK, repoint all writers + views | trust/architecture | **O45 fires (engine+T18 merged) → this is the first cutover domino** | OPEN |
| O24 | ⭐ **Keystone CSV importer (`hours_import.js`) has NO dedup + NO billed-period guard** — re-upload doubles hours; can import into a billed month. Legacy importer protects both → parity work, part of the hours unification | trust/risk | with O23 | OPEN |
| O25 | **External-access sprawl: 3 portal modules + 2 identity models** (`customer_portal` = customer-role+keystone; `client_portal` v1 legacy; `client_portal_v2` separate token identity, most complete). Consolidate to ONE = v2 token-identity + keystone data | architecture | cutover / portal roadmap | OPEN |
| O26 | **Residual (low): hidden training content's static bundle is still fetchable by a determined devtools user** (complete-hide UX shipped in WP-A; true per-chunk asset gating deferred) | note | only if truly-restricted content is ever needed | OPEN (low) |
| O27 | **Map data is generic-KV JSON** (`map_store` blobs) — fine for estimates, limiting for materials-sync/per-feature status/multi-user. Relational-vs-KV fork decision at map productionization | architecture (med) | map productionization (post-D016 delivery) | OPEN |
| O30 | ⭐ **The cluster has NO config UI — `settings.html` is a stub; ALL config lives only in legacy `admin.html`** (pricing/jobs/portal-access/invoice-templates/CCs/client-links/staff/user-CRUD). **GUARDRAIL: admin.html cannot be deleted until the cluster Settings page is built** | cutover/risk | cutover planning; BEFORE any legacy purge | OPEN |
| O31 | **Hours-audit retention mismatch:** `time_entry_audit` hard-deleted at 18mo vs general `audit_log` 3yr+ append-only — may be short for RUS (~3yr) retention | retention (low) | with hours work / RUS compliance review | OPEN |
| O34b | **Perm-flash residual:** restricted PAGES still serve 200 HTML to non-staff → client-gated render-then-hide flash (the DATA leak half was fixed + deployed). Fix = server-gate page routes OR default-hidden→reveal | UX/security (low) | with the I10 nav/redesign structural pass | OPEN |
| O37 | **Real-time phases 2–3:** phase 2 = list/project/map live-REFRESH (broadcast on writes → refetch, existing SSE); phase 3 = true concurrent map CO-EDITING (large, CRDT/locking — deferred, do NOT block on it). Phase 1 (training visibility) shipped | architecture | phase 2 after the launch set; phase 3 much later | OPEN |
| O40 | **Test-fixture junk in the shared prod DB** (21 of 23 clients are `casc-client-*`/`billing-client-*` fixtures; pv_/wpd_ test users already deactivated). Needs **FK-aware deletion** (app endpoints or careful bottom-up script — some SAs billed → invoice FK RESTRICT), not a blind cascade. Unblocked (backups on) | hygiene | focused cleanup pass | OPEN |
| O41 | **Dead SSE code: construction roles** (`_sse.js` handles `construction_manager`/`construction_engineer`/`team:construction`; `VALID_ROLES` has none) | consistency (parked, low) | at System F / roles work | OPEN |
| O42 | **Rotate the Railway DB credential — ACTIVE.** Pasted plaintext into the Auditor's chat **×2 this session** (Auditor flagged 2026-07-01, prefix `xKwaGL…`); it's the shared/prod cred. **Rotate, then update Planning's local `.env`** so Planning keeps live-verification access. NB distribution to workers is moot — the sandboxed workers can't use it anyway (Auditor sandbox blocks raw Postgres TCP); rotation is pure hygiene | Carter DECISION | 2026-07-02 | **CLOSED — Carter: "DB cred will not be rotated"** (accepted risk; the leaked cred stays; sandboxed workers can't use it anyway) |
| O43 | **GitHub Actions billing will NEVER be fixed** (Carter, 2026-07-01) → **CI is permanently dead.** Standing consequence: local/manual verification is the PERMANENT model — no agent waits on or assumes CI; deploy-gating = `npm test` run LOCALLY + a live-preview pass before Planning merges. Voids O9(a). Recorded in INVENTORY + memory | ⚠ CARTER → ANSWERED | — | CLOSED (→ archive next sweep) |
| O44 | **COMMITMENT — I9 decision (RUS daily-paperwork tool) the moment the next CC is approved:** build-or-consciously-skip, immediately. RUS likely ends ~end-2026; build+adopt ≈1–2mo; the tool is valuable exactly when construction resumes. Parked-indefinitely = a silent no | commitment (trigger) | **Carter says the next CC is approved** (also re-raise on any RUS-resumption mention) | OPEN |
| O45 | **COMMITMENT — start the hours-unification cutover domino (O23/O22/O24) IN PARALLEL with the content cadence** once the assessment engine + T18 are merged. Content authoring is a gated content track; the CEO has architecture capacity — don't serialize the whole launch-set first | commitment (trigger) | **engine merged 2026-07-01 ✓; awaiting T18** | OPEN |
| O46 | **`schema.sql` not regenerated for migration 0082** (assessment engine). `schema:sync` validated 0082 applies cleanly (82 migrations, 0 skipped) but the pg_dump step ENOENT'd — **no pg_dump/psql/PostgreSQL client in Planning's env.** Migrations are the source of truth + 0082 auto-applies on deploy; CI dead (O43) so no schema-drift gate fires → drift is documentation-only. **Install PG client tools in Planning's env** (schema:sync is a standing Planning-at-merge step), then regenerate + commit schema.sql. ⚠ don't hand-generate with a mismatched pg_dump version (spurious diff) | hygiene/infra | next migration merge / when PG client installed | OPEN |
| O47 | **Comms-path gap stalling BOTH builders (CEO flagged 2026-07-02 04:45).** C1/C2 dispatches live on `threads/cN.md` on the **CEO branch** (D022), but a bare "pull main" nudge makes builders pull **main** → C1 never saw its `T01-final` go-ahead, C2 never saw its red-team signal; both stuck "holding, no new instruction." Deadlock is structural — the watcher-swap they need is ALSO on the CEO branch they can't see. FIX: (a) BOOT_PROMPTS C1/C2 hardened with a "NUDGE TRAP: nudge≠main, fetch the CEO branch" note ✓ (this session); (b) **Carter must re-nudge C1+C2 with the correct target** — `git fetch origin claude/ceo-fresh-instance-boot-u2zw28` → read `threads/cN.md` THERE + arm the persistent Monitor on that branch. Bigger idea (parked, see ideas): a stable `planning/CEO_BRANCH.txt` pointer on main so builders discover the current CEO branch without a hardcoded slug | prompt fix done (this session); builders turned this round (persistent Monitor fired 3×) | monitor next boot cycle | RESOLVED-pending-confirm |
| O48 | **5 live wrong regulatory citations in PUBLISHED T01 lesson PROSE** (C1 caught; not C1's errors): RUS Form 219 misapplied as telecom close-out in L04/L05/L06/L09; 1751F-630 (splice-closure/OSP-scope) + 1753F-201 (materials list) misapplied; pole-fee cited **47 CFR 1.1411** (= "access timeline", wrong). Live accuracy defect on our one live gov topic. RULING (D024, this session): **gated fast-follow increment (inc5), NOT folded into inc4**; **T01 stays live meanwhile** (errors are explanatory prose, not the graded assessment — C1 kept unverified specifics out of the pools; reversible; pulling the DAG entry-point over 5 cite-numbers is disproportionate — flagged to Carter for override). **Planning's primary-source pass sets the CORRECT citations** — C1's fixes are NOT auto-trusted (1.1409 is itself imprecise; correct anchor likely §1.1406 + §§1.1409/1.1410). Fix pass = C2 edits the T01 JSX after its red-team, using Planning-verified cites | content accuracy (live) | fast-follow after inc4 | **RESOLVED 2026-07-02** — the full O48 prose set (incl. Form-219) fixed via inc5 + the **D033 fresh-grep** pass (C1 found 8 occurrences — incl. the 6 the Auditor's named inventory had missed); Form-219 correctly reframed as Electric-program (7 CFR 1726.405 / 1753.49 for telecom). Auditor re-verified clean; T01 merged + render-clean (D028) + flipped live |
| O49 | **Institute a RENDER/USER-TEST pass (D028) + fix its enabler.** A published T01 lesson (L06) crashed live for trainees (`<TimelineSequence>` w/o `correctOrder`); nobody was clicking through lessons. Hotfixed (`666fe1a9`) + component hardened. **Enabler BLOCKED:** Planning has no `ADMIN_PASSWORD` in its local `.env` → can't drive the authed training SPA in the preview to render-test (and won't pollute prod with a throwaway account). FIX one of: (a) add a local-only preview-admin auto-login to `scripts/_preview_boot.js` (not-used-in-prod), or (b) Carter puts `ADMIN_PASSWORD` in Planning's local `.env`. **Also:** `build:osp` should clean `public/training/assets` before copy (orphaned old hashed chunks accumulate on `cp`). **Also:** CEO greps ALL lessons for `<TimelineSequence>`/`<Sortable>` missing `correctOrder` (now guarded → shows "unavailable" not crash, but content-fix them) | process/quality | now | **AUTH-ENABLER RESOLVED** (Carter OK'd — created a `planning` admin, separate from `admin`; verified T01-L06 renders clean via a real authed render; user-test path works. Next Planning re-sets the `planning` password via the one-off pg+bcrypt script pattern — do NOT commit the password). **Still open:** `build:osp` should clean `public/training/assets` first (orphaned chunks); CEO greps lessons for missing `correctOrder`; render-test each topic before merge (D028) — **DONE for the launch set 2026-07-02**: all **54** T01/T18/T02/T03/T04 lessons render with full content via the `planning`-admin preview, **0 console errors, 0 crashes/blanks** (T01/06 — the prior white-screen — now renders full; capstones show as stripped review pages). `build:osp` still doesn't auto-clean `assets` (did `rm -rf` manually this merge — remaining nicety) |
| O50 | **⚡ DOMAIN MIGRATION → `launchfiber.app`** (Carter 2026-07-02, TOP PRIORITY). New canonical domain. **"origin not allowed" = the domain wasn't in the `ALLOWED_ORIGINS` env var** (CORS gate `server.js:84` + CSRF gate `:135`; verified no CSP/hardcoded-domain block). **INSTANT FIX (Carter, Railway env — no deploy):** add `https://launchfiber.app` to `ALLOWED_ORIGINS` (exact `https://`, no trailing slash) on the MAIN service (PORTAL_MODE empty). **Follow-ups (Planning-owned code, VERIFIED before deploy):** (a) harden CORS to always allow same-origin (`server.js:80` — a domain swap then needs no env change; today it rejects same-origin if the host isn't listed); (b) `SPLICE_PUBLIC_URL` default → launchfiber.app (`routes/splice.js:93`); (c) desktop app hardcodes the old domain (`desktop/main.js:23` + renderer); (d) doc sweep (README `portal.launchfiber.com` refs). Cookie is host-only → old-domain sessions re-login on the new domain (expected). | infra/migration (TOP) | now | **OPEN — env fix = Carter's action; code follow-ups = Planning** |
