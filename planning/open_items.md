# Open Items — questions, pending decisions, parked recommendations (re-raise log)

> Owned by Planning. So nothing gets dropped and everything gets **re-raised at the right moment** — a stopping point, the relevant work area, or when Carter asks something related. Status: OPEN / ANSWERED / DONE / DROPPED. Carter (2026-06-28): *"we need lots of logs for the organizational pieces to actually work."* Last updated 2026-06-28.

| # | Item | Type | Re-raise when | Status |
|---|---|---|---|---|
| O1 | **Planning does a first-hand live user-test** of the app, cross-referenced vs `INVENTORY` (verify built-vs-documented firsthand; trust own eyes over agent reports) | recommendation | right after the People fix deploys (verify it + everything in one pass) | PROPOSED 2026-06-28 |
| O2 | **Feature deep-dive** (post-training roadmap; Job Board first) | parked thread | once Carter user-tests the People foundation on live | PARKED |
| O3 | **Job Board unit:** does "Service Area" fit small non-RUS permits, or a lighter Project/Permit unit? (+ at-a-glance fields, + billing-status states) | open question | when the Job Board deep-dive resumes | OPEN |
| O4 | **Billing submission samples** (RUS inspection / RUS design / non-RUS permitting) + the RUS code list | input from Carter | when billing (System A) starts | OPEN |
| O5 | **Does Workforce export CSV?** (+ a sample) | input from Carter | when hours (System D) starts | OPEN |
| O6 | **Map version** timing (boss-dependent) | input from Carter | when map integration (System C) starts | OPEN |
| O7 | **Cockpit alert thresholds** (fixed-fee %, RUS cap %, utilization) | input from Carter | when cockpit (System E) starts | OPEN |
| O14 | ✅ **VERIFIED + REFINED (chunk 18): SA hard-delete = config-loss + hours-orphaning (NOT hours-deletion), no undo; billed SAs FK-protected.** Schema-confirmed: deleting a `service_area` CASCADEs to its jobs/routes/materials/assignments (config + $ amounts destroyed) BUT `time_entries.service_area_job_id` is **SET NULL** (hours rows survive, orphaned/unattributed — not deleted), AND `invoices`/`invoice_items` FKs are RESTRICT (a BILLED SA can't be deleted). `DELETE /api/service-areas/:id` (chunk 03) is still a plain delete with **no undo bucket**. **Fix = wire `saveUndoBucket`+`showUndoBar` (infra+UI exist, chunk 15).** Severity downgraded high→**medium** (hours not destroyed; billed protected) but still a real no-undo config-destroy. | risk → VERIFIED (chunk 03→18) | before relying on SA delete; small fix | OPEN |
| O17 | **THREE "service-area"-ish tables coexist** — `service_areas` (keystone), `concentrators` (old rollup), `ec_service_areas` (EC cascade picker). Naming + model overlap; cutover must reconcile/retire the legacy two. | architecture (chunk 06) | cutover planning | OPEN |
| O18 | **PARALLEL legacy/keystone tables — the cutover's true scope** (generalizes O17). The keystone migration built *parallel* tables instead of migrating in place, so legacy+keystone coexist across: SA (`service_areas`/`concentrators`/`ec_service_areas`), **contracts (`contracts` vs `construction_contracts`** — also a naming collision: EC API calls `contracts` rows "construction-contracts"), dashboards (legacy `/api/dashboard` vs `/overview`), **billing (5 invoice-creation paths — 4 legacy `bill-multiple`/`generate-monthly-invoice`/`bill-and-clone`/`:id/bill`-orphan + 1 keystone `/run`; O15)**, **money reporting (legacy `revenue.js` vs keystone `projections.js`)**, **projections (legacy `automation.js` buildPscRus/InspectionRevenue/MonthlyBillingDraft — RICHER: sparklines/forecast/monthly-draft — vs keystone `projections.js`; PARITY GAP, chunk 17)**, tree (legacy `projects` vs `/projects-tree`). Keystone money-trio (projections+budgets+billing_keystone) is internally coherent; debt = the legacy duplicates clinging to the projects tree. **Need ONE reconciliation map** (which table/path wins, migration path, what retires) before cutover — piecemeal will strand data; cutover must PORT richer legacy features (sparklines, dedup, etc), not just reroute. | architecture (chunk 06–07, 17) | cutover planning (the foundation of it) | OPEN |
| O16 | ✅ **REFINED (chunk 07a): the invoice↔SA link EXISTS on the keystone path.** `invoice_items` has BOTH `project_id` (legacy) + `service_area_job_id` (keystone); `invoices` has `service_area_id`+`engineering_contract_id`. `billing_keystone /run` sets all → those invoices ARE per-SA/EC/program joinable. Real problems: (a) `service_areas.js :id/bill` makes ORPHAN invoices (no link at all), (b) `money_view`/program-financials derive from `service_area_jobs` status instead of joining the keystone invoice columns. **Fix = retire orphan paths + repoint reporting at `service_area_job_id`/`service_area_id`** (lower-effort than first thought). | architecture → ACTIONABLE | at billing rebuild | OPEN (scoped) |
| O15 | ✅ **RESOLVED (chunk 07a): canonical = `billing_keystone.js` (progressive earned−billed ledger).** Three paths: #1 legacy `billing.js` (projects, retire w/ projects tree) · #2 `service_areas.js :id/bill` (orphan invoices — retire or delegate) · #3 `billing_keystone` (keystone, proper SA/EC links — **THE engine**). server.js line 760 confirms coexistence is intentional-during-cutover. Full table + recommendation in `codebase/07-billing.md`. | architecture → RESOLVED | billing rebuild executes it | OPEN (recommendation banked) |
| O19 | **BUG: `GET /api/billing/report` double-registered.** Defined in billing_keystone.js (l163, `{group,rows}`) AND billing.js (l622, `{year,month,ytd,…}`). server.js mounts keystone first (761) → legacy second (921), so Express serves **keystone**; billing.js `/report` is **dead**. If `billing.html` calls it expecting the legacy shape, it silently gets the wrong shape. | bug (chunk 07a) | UI/deepening pass — verify billing.html; fix in billing rebuild | ✅ **DOWNGRADED→LOW (U7/U8 live): NOT user-facing — nothing calls `/api/billing/report`.** billing.html uses `/api/billing/invoices` (invoice list); billing-keystone.html's Report tab uses the keystone `/report` (which wins the mount order). The shadowed legacy `/report` is **dead/unused code** — cleanup only, no broken feature. |
| O20 | ⭐ **#1 BILLING-CUTOVER BLOCKER (chunk 07b): invoice DATA assembly + the RUS PDF are legacy-`projects`-ONLY.** `invoice_generator.js` (the PSC RUS PDF — the actual government deliverable) reads `projects`/`concentrators`/`time_entries.project_id`, **zero `service_area_job_id`**. So a `billing_keystone`-created invoice has **no submittable RUS PDF**, renders blank in the legacy `/api/invoices` list, and isn't unbilled by void. Canonical ENGINE (keystone ledger) ⊥ canonical DELIVERABLE (RUS PDF). **The billing rebuild must PORT invoice data assembly to `service_area_jobs`** (hours via `time_entries.service_area_job_id`, WO# via SA, contracts via EC) — not just switch engines. | architecture/risk (chunk 07b) | billing rebuild — do this BEFORE retiring legacy projects | OPEN |
| O21 | ⭐ **CONFIRMED GAP (chunks 08+19): the RUS DAILY FIELD PAPERWORK is not built anywhere.** Verified: desktop = web-app shell + file-sync (no forms); inspection.js = read rollup; the only "RUS form" refs are TRAINING lesson content. So the daily inspection report/form tool — THE RUS-exclusive deliverable + core pain (`project_business_reality`) — is done OFF-platform today. **High-value greenfield RUS feature; field-facing → mobile/PWA (`feature_mobile_app`), feeding keystone (new daily_reports table / service_area_jobs), NOT the Windows desktop.** Promoted to idea I9. | confirmed gap (08→19) | when Carter prioritizes RUS field tooling | OPEN |
| O22 | ⭐ **TRUST (chunk 09): hours rounding is silent AND INCONSISTENT across entry paths.** Manual/CSV writes snap to nearest 0.25 (`snapHoursToQuarter = Math.round(n*4)/4`, POST/bulk/PUT + both importers): `1.1→1.0`, `1.13→1.25`. **But the TIMECLOCK does NOT snap** — clock-out stores 2-decimal raw hours (`3.47`) via its own INSERT (bypasses the snap). So same person's hours differ in granularity by method, and **editing a timeclock entry in the Hours tab silently snaps it (3.47→3.5).** Neither "NO ROUNDING EVER" nor a single grid policy. Fix = ONE policy at ONE chokepoint (recommend store raw/2-dp + reject non-grid manual input; never silently rewrite on edit). **NOTE (chunk 20): the snap is TEST-LOCKED (`hours_quarter_snap.test.js`) = intentional, not a bug → this is a POLICY decision for Carter (silent-snap vs reject-non-grid), and changing it updates that test.** | trust/billing/policy (chunk 09, 20) | surface to Carter as a policy choice | OPEN |
| O23 | ⭐⭐ **TRUST ROOT-CAUSE (chunk 09): hours SPLIT-BRAIN — legacy `time_entries.project_id` vs keystone `time_entries.service_area_job_id`.** Three legacy write paths (time_entries.js, hours_csv importer, **the timeclock** — field employees' daily hours) all write `project_id`; keystone path (service_areas.js time-log + hours_import) writes `service_area_job_id`. Two view sets (legacy `/api/time-entries`+`/api/inspection` vs keystone `/api/hours/summary`+billing ledger). An entry lives in ONE world, invisible in the other → **different totals on different screens for the same person.** Almost certainly THE reason Carter distrusts hours. Cutover must unify on one column + migrate. Ties O16/O18. **✅ schema-verified (chunk 18): BOTH columns exist (project_id FK RESTRICT, service_area_job_id FK SET NULL), NO XOR/CHECK constraint forcing exactly-one — split is app-enforced only; `budgets.budget_scope_exactly_one` is the precedent CHECK to add post-unify.** | trust/architecture (chunk 09, verified 18) | surface to Carter; foundational to cutover + hours trust | OPEN |
| O24 | ⭐ **TRUST/"nothing can break" (chunk 09b): the KEYSTONE CSV importer (`hours_import.js`) has NO dedup + NO billed-period guard.** Its commit INSERTs every row with no check → **re-uploading the same CSV DOUBLES the hours**, and it can import into an already-billed month. The LEGACY importer (`hours_csv.js`) protects against both (match-key dedup + billed-period lookup) + has a persistent review queue + auto-create. So the cutover is a feature-PARITY project, not a reroute — keystone must regain dedup + billed-guard before it's trusted/canonical. | trust/risk (chunk 09b) | before keystone importer becomes canonical | OPEN |
| O25 | **External-access SPRAWL (chunk 10): 3 portal modules + 2 identity models.** `customer_portal.js` (customer-role+`customer_clients`; only one on KEYSTONE `/service-areas` + invoices + map + admin client-progress) · `client_portal.js` v1 (same identity, legacy projects, oldest) · `client_portal_v2.js` (SEPARATE identity: `client_organizations`/`client_users`/`client_tokens`; most complete — branded tokens, doc-exchange, approvals; legacy projects). Consolidate to ONE = v2 token-identity + keystone data + invoices/map/approvals. Extends O18 to the portal/identity layer. | architecture (chunk 10) | cutover / when customer-portal roadmap resumes | OPEN |
| O26 | **Minor/by-design (chunk 11): training content visibility is CURATION, not security.** Lessons ship as static bundles in `/training/`; `my-content` only filters the SPA nav + fails OPEN. A "hidden" lesson's JS is still fetchable by URL. Fine for non-secret training, but **"gated" ≠ "inaccessible"** — if any content ever needs true restriction (paid cert, NDA material), it requires server-enforced delivery, not nav filtering. | note (chunk 11) | only if truly-restricted content is ever needed | 🟡 **UX-HIDE SHIPPED (WP-A, 2026-06-30): hidden lessons are COMPLETELY GONE — no lock screen** (Carter's override). LessonRouter doesn't even load a hidden lesson's chunk + a direct URL **redirects** (to course home if the subject's visible, else the chooser) with no hint it exists; nav omits them; server `/my-content` returns only visible ids. **RESIDUAL (low, still OPEN):** the static bundle is still fetchable by a determined devtools user (true per-chunk static-asset gating deferred) — fine for non-secret training; do it only if content becomes truly restricted. |
| O27 | **Map productionization fork (chunk 12): map data is generic-KV JSON, not relational.** The FRM map persists whole-plan blobs in `map_store` (`frm_pts_<plan>`/`frm_segs_<plan>`), parsed only by `computeEstimate`. Fine for pricing/estimate; **limiting for materials-sync (map→`service_area_materials`), per-feature status, incremental + multi-user editing.** When the map graduates from POC, the key decision = move structures/spans to relational tables vs keep KV. Ties chunk-03 "materials sync deferred" + I6. | architecture (chunk 12) | when map productionization starts | OPEN (med) |
| O28 | ✅ **RESOLVED (Carter confirmed 2026-06-30): a persistent volume IS mounted on the service AND the Postgres DB.** So uploads (docs/photos/DWG to `UPLOAD_DIR`) survive Railway redeploys — no silent data-loss. (Was: HIGH data-loss risk if no volume mounted, since containers are ephemeral.) Built-in detector `GET /api/_debug/uploads` remains available to spot-check `missing_file_count` if ever needed. | risk/verify (chunk 13) → RESOLVED | — | CLOSED |
| O29 | **CSRF posture (chunk 15, low): no CSRF token.** The shared `api()`/`apiUpload()` auth = cookie JWT + Bearer fallback; CSRF protection relies on `lfs_session` **SameSite** + the CORS `ALLOWED_ORIGINS` allowlist. Verify SameSite is Lax/Strict (chunk 02) — if `None`, state-changing requests are CSRF-exposed. Likely fine (modern default). | security/verify (chunk 15) | quick check at chunk 02 recap / security review | OPEN (low) |
| O30 | ⭐ **CUTOVER (chunk 16): the new cluster has NO config UI — `settings.html` is a stub (loads only app_nav.js); ALL settings live ONLY in legacy admin.html.** Stranded admin.html-only: pricing(I4)/jobs/portal-access+capabilities/invoice-templates(I5)/construction-contracts+catalog/client-links/staff/user-CRUD+delete + legacy inspection/revenue/billing/pipeline views. ⇒ admins must bounce to admin.html to configure anything. **GUARDRAIL: admin.html CANNOT be deleted (Phase D pre-authorized legacy-delete must EXCLUDE it) until config is rebuilt in the cluster** — deleting it removes the only way to configure the platform. The keystone Settings page must be built: pricing/jobs/portal-access/templates/CCs/client-links/staff/user-CRUD. **✅ CONFIRMED LIVE (U15): settings.html = Appearance + Quick links + System info ONLY, zero config controls.** | cutover/risk (chunk 16, confirmed U15) | cutover planning + BEFORE any legacy purge | OPEN |
| O31 | **Hours-audit retention mismatch (chunk 17, low): `time_entry_audit` is HARD-DELETED at 18mo (meaningful) / 14d (trivial)** by the automation scheduler, while the general `audit_log` is append-only + never deleted (3yr+). So the **hours** change-history is less durable than the general audit — for RUS/government (≈3yr retention) 18 months may be short. Reconcile if hours audit must meet RUS retention. | retention (chunk 17) | with O22/O23 hours work / RUS compliance review | OPEN (low) |
| O32 | ✅ **MOSTLY RESOLVED — per-lesson visibility BUILT (2026-06-29).** Carter directed the per-lesson show/hide feature; Planning built it (canonical "Published" preset + `GET/PUT /api/training/published` + "Lesson visibility" modal in training-admin.html; new trainees default to the Published set, OSP fallback until configured; admin sees all). Replaced the interim OSP-track stopgap with this proper mechanism; verified end-to-end vs prod DB. **✅ CURATION RESOLVED (Carter 2026-06-30):** Carter curated it himself — **T01 (Fundamentals) is the published demo + the starting point** for anyone who needs training tomorrow; everything else stays hidden until it clears the gate. Matches the plan's hide-unverified intent. Standing: as each new subject clears the gate, Carter green-lights → publish it (check its lessons in Lesson Visibility). **Near-term priority he set: get more subjects out soon.** | built → DONE (T01 published) | re-raise as subjects clear the gate (pending-flips queue) | CLOSED |
| O33 | **App-wide light/dark toggle: label + default (Carter 2026-06-29).** (a) Every page's toggle is a sun/moon icon → make it **text "Light Mode" / "Dark Mode"** (clearer). (b) **Default = DARK** for new accounts + any login with no saved preference; only an account that explicitly set/preferred light starts light. | ⚠ CROSS-IMPACT: theme handling is DUPLICATED across pages — `app-shell.js` (localStorage `lfs-theme`, server `/api/auth/me/theme`; current default = saved→prefers-color-scheme→light) AND `training-admin.html` (separate `lfs_theme` underscore key) AND the osp-training SPA (own theme). "Every page" = unify/touch all three; the I10 redesign should consolidate theme into one place. Server pref for light-set accounts must still win. | quick win + cleanup (Carter) | with I10 redesign, or as a small standalone batch | ⭐ **SUPERSEDED (2026-06-30): evolved into a full THEME PICKER.** Carter wants a user-selectable theme catalog (multiple dark + light themes); the **picker button REPLACES the light/dark toggle** entirely. Default = **B (Graphite steel, dark)**. The "Light/Dark text label" ask is moot (it's a theme menu now). Build = theme engine (data-driven token sets + per-user pref id, extends `/api/auth/me/theme`). Full spec in `redesign_ui.md` → "THEME-PICKER reframe". O33's "default dark" intent survives (default B). | OPEN → folded into I10 theme engine |
| O34 | ⭐ **BUG / SECURITY (Carter 2026-06-29): flash of perm-locked data — and a REAL authz leak found under it (deep-dive verified live).** TWO parts: **(a) FLASH:** restricted PAGES (`/people.html`, `/money.html`, `/invoices.html`, `/admin.html`) all serve **200 full HTML to a trainee** → client-gated render-then-hide → the flash. Fix: server-gate the page routes OR hide-before-paint (CSS default-hidden→reveal), never render-then-hide. **(b) REAL DATA LEAK (worse than cosmetic):** several reference GETs are **"any-auth"** and returned business data to a TRAINEE (verified, role=trainee, 200): **`/api/clients`** (client names/notes), **`/api/engineering-contracts`**, **`/api/projects`** (names+client), **`/api/contracts`**, **`/api/pricing`** (RATES!), `/api/project-types`. (Correctly 403: `/api/jobs`, `/api/staff/all`, `/api/service-areas`.) ⚠ CROSS-IMPACT (the nuance): these are any-auth ON PURPOSE — the design/permitting/timeclock portals' cascade pickers need them → **don't lock to admin (breaks portals); restrict to STAFF roles (exclude `trainee` + `customer`)**, ideally trim fields. **This is the actual "perm-locked data" Carter means.** Customers likely leak too (verify). | bug/security (Carter, deep-dive) | fix soon — real leak (clients/projects/rates to trainees) | ✅ **FIXED + DEPLOYED (Planning, 2026-06-30, commit `46bb2a6d` on main → Railway).** The CEO never shipped it (routed 06-29; Planning verified still-live in code 06-30 → fixed directly). Mechanism = new `requireStaff` in auth.js = `requireAuth(STAFF_ROLES)` where `STAFF_ROLES = VALID_ROLES − {trainee, customer}` (D013-clean, auto-covers future roles). Applied to the leaky GETs in clients/pricing(+lookup)/engineering_contracts(+5 sub-resources)/projects(+:id,+monthly-hours)/project_detail/contracts/concentrators/project_types(was fully unauth via `{}` stub)/potential_permits. **part (b) "flash":** the page-shell render-then-hide is NOT yet addressed (pages still serve 200 HTML to non-staff → cosmetic flash) — the *data* leak (a, the worse half) is closed; do (b) in the I10 nav/redesign (server-gate page routes OR default-hidden→reveal). Regression guard = `tests/o34_staff_gate.test.js` (5/5). NOT done: field-trim on the responses + the splice/dwg-sync any-auth GETs (separate, not in the named leak set — follow-up). | DONE (a); (b)-flash OPEN |
| O35 | **BUG (U16): Audit log page broken — `GET /api/audit/log` → 500.** audit.html renders (filters + table) but the data load fails ("Failed to load audit log"). Server log: **`[audit-view] column "created_at" does not exist`** — the audit-view query references `created_at` but `audit_log`'s timestamp column is **`at`** (chunk 14 `_audit.js`). **TRIVIAL FIX** (rename `created_at`→`at` [or the real column] in the audit-view query in `routes/_audit.js`/the audit route + verify ORDER BY/pagination). `/api/_admin/timeclock-audit` works (200). CEO-lane quick fix. | bug (U16) | quick fix (CEO or inline) | ✅ **FIXED + DEPLOYED (Planning, 2026-06-30, commit `46bb2a6d`).** `audit_view.js` now `SELECT id, at AS created_at … ORDER BY at` (alias keeps the frontend JSON contract identical). | CLOSED |
| O13 | ✅ **RESOLVED (chunk 14): migrations DO auto-run on deploy.** railway.json + nixpacks use `node server.js` → `start()` → `runMigrations()`. Pushing a `migrations/*.sql` auto-applies on next deploy (prestart is skipped but start() runs the runner itself). Corrected `reference_deployment` memory. | VERIFY → DONE | — | CLOSED |
| O9 | ✅ **REVISED DOWN (chunk 20): the regression net largely EXISTS** — 64 test files cover money/hours(incl quarter-snap+attribution)/keystone/security/schema-drift + integration/ + browser/ E2E. Remaining: (a) VERIFY `npm test` runs in CI/pre-deploy (a suite only protects if it gates deploys — chunk 14 saw deploy_preflight/validate_schema; confirm test run too); (b) add the 2 gap tests — keystone SA-delete cascade (O14) + legacy↔keystone reconciliation (O23). Not the "build a QA net from scratch" I'd assumed. | recommendation → REVISED (chunk 20) | verify CI gating; add 2 gap tests | OPEN (reduced) |
| O10 | **Staging-before-live** — `main` auto-deploys straight to prod; add a pre-prod check so nothing broken reaches the team | recommendation (Planning) | when we discuss deploy safety | OPEN |
| O11 | **Automated data backups** — verify they exist / establish them (real revenue + government data) | recommendation (Planning) | soon — it's a standing risk | OPEN |
| O12 | **Auditor role not yet stood up** — defined but no instance/doc ready for when content gating produces audit targets | recommendation (Planning) | before the first content audit (T01) | OPEN |
| O8 | **16 non-`agent` stale branches** — keep/delete each (`claude-2/*`, `claude-4/*`, `claude-5/*`, `claude/*` randoms, `feature/*`, `orchestrator-*`, `worktree-*`); **KEEP `claude/ceo-onboarding-planner-rfg0rc`** (active CEO) | hygiene | at a cleanup stopping point | OPEN |
| O36 | ⭐⭐ **TRAINING VISIBILITY is broken + over-complex (Carter 2026-06-30, multiple bugs).** Symptoms: (a) new signup sees the "default OSP" but it's **ALL** lessons, not a curated set; (b) setting only 1 lesson visible **changes nothing**; (c) **can't revoke seen content** — gave a user the Cert track, removed it, it **persisted**; (d) the **flash** — new user sees 3 tracks for ~0.5s every load. **ROOT CAUSE (code-verified):** `routes/training.js loadUserVisibility` has **5 overlapping "base" concepts** (`all`/`preset`/`published`/`default`/`none`) + a **HARD-CODED OSP fallback** (`defaultVisibleLessons`) + my own "Published preset seeded with all OSP" — all conflicting; AND `osp-training/src/hooks/useMyContent.js` **fails OPEN** (loading/error → shows everything) and filters **client-side over static bundles** (O26) so hidden content isn't truly gated → flash + persistence. **Carter's spec:** ONE simple model — a **settable new-user default** (NOT hard-coded), **"Publish" lessons AND tracks**, **give/take per person that actually hides** (revoke wins even if already seen), **server-enforced** (no flash), **simple/non-conflicting**. CEO-scope rebuild; unblocks the content cadence (O-train). | bug/architecture (Carter) | NEXT — top training priority; route to CEO | ✅ **DONE + MERGED (2026-06-30, main `b172cd87`).** CEO built, Planning verified live + merged. ONE resolver `visible = admin?all : (default ∪ SHOW − HIDE) ∩ published` (migration `0080`: singleton `training_visibility` pins `__published__` + `__default__`; default = `track:osp` [flows w/ publish]; T01 curation preserved; legacy `base='all'` retired). Server-authoritative `/my-content` (503 on error, never `all:true`); `useMyContent` de-fail-open + skeleton (no flash); admin UI = Publish (lessons+tracks) / New-user-default (subjects) / per-user grant-revoke (hide-wins). **VERIFIED live (preview, real accounts):** fresh trainee = only published OSP/T01, no ISP/Certs; revoke→gone live no-refresh; restore→back live; hidden-lesson URL→redirect (no lock screen); admin=all. |
| O37 | ⭐ **REAL-TIME SYNC required (Carter 2026-06-30): perms/visibility changes + map editing + project editing apply WITHOUT refresh.** Foundation EXISTS: `_sse.js` `GET /api/events/stream` w/ `admin`/`team:*`/**`user:<id>` (reserved — built for this)** + `broadcast()`; write handlers already broadcast (staff.js etc.). **PLANNING PUSH-BACK (phasing):** (1) **live perm/visibility sync** (admin changes a user's access → broadcast `user:<id>` → SPA refetches my-content, no refresh) + **list/project live-refresh** (broadcast on writes → re-render) = achievable NOW on existing SSE; (2) **map live-refresh** (viewers see saved changes) = medium; (3) **true concurrent map CO-EDITING** (multi-cursor, conflict-free merge) = a large separate project (CRDT/OT or locking) — do NOT block (1)/(2) on it. Pairs with O36 (visibility real-time is part of that rebuild). | requirement/architecture (Carter) | NEXT (phase 1 w/ O36); phase 2/3 sequenced | 🟡 **PHASE-1 DONE (2026-06-30, `b172cd87`):** training-visibility real-time live via SSE — `_sse.js` `training` channel (global publish/default) + `user:<id>` (per-user grant/revoke) → SPA `useVisibilityStream` (EventSource → invalidate my-content) → updates with NO refresh. **VERIFIED live** (revoke/restore round-trip, no reload). **Phase 2** (list/project/map live-refresh) + **Phase 3** (true concurrent map CO-EDITING — large, deferred) still OPEN. |
| O38 | ⭐ **FULL UI redesign not finished — do ALL of `redesign_ui.md`, not piecemeal (Carter 2026-06-30, disappointed twice).** Still broken: (a) **sun/moon toggle persists on pages** (not replaced by the picker everywhere); (b) operations cluster has **no centered-logo topbar** (app_nav.js rail only); (c) missing **nav bubble icons** (hover scale ~1.2× + name tooltip), **push-sidebar reflow**, **left hamburger**, **logo replaces "Launch"/"Projects" wordmark text at top**. **Fix:** mount the prod-proven `AppShell.mountTopbar()` (centered logo + theme picker top-right + user menu — already works on launcher) onto the operations cluster via `app_nav.js`; add hamburger+push-reflow+bubble icons; **purge the sun/moon `#themeToggle` everywhere** + per-page legacy theme JS. Apply to ALL surfaces (cluster + SPA + portals). | UI/redesign (Carter) | NEXT (parallel track to O36) | ✅ **DONE + DEPLOYED (WP-C, merged `85983c04`, 2026-07-01).** `app_nav.js` mounts `AppShell.mountTopbar` on the operations cluster (centered logo + theme picker top-right + user menu); **sun/moon purged everywhere** (removed from billing_view/pipeline_board/service_areas_ui + the page HTML, not just hidden); left hamburger → push-sidebar reflow; nav bubble icons + `data-tip` tooltips; SPA header "OSP"→"Training". 4 themes. **Verified live** (Planning preview: topbar+logo+picker render, hamburger collapses rail, 0 visible sun/moon). RESIDUAL (deferred, low): the bigger structural billing-tab 3→2 consolidation is separate cutover work. | CLOSED |
| O39 | **Username not freed on inactive/delete — "username taken" for legacy/inactive accounts (Carter 2026-06-30).** `users.username` is `UNIQUE NOT NULL`; soft-delete/deactivate keeps the row → username stays reserved; register catches 23505 → "Username already taken." **Fix:** on deactivate/soft-delete, **release the username** (rename to a tombstone e.g. `<name>__inactive_<id>` or NULL+constraint change) so it's reusable; + a one-time migration to free already-inactive/legacy usernames. Verify against the People delete flow (chunk 04). | bug (Carter) | quick fix — bundle w/ the People work | ✅ **DONE + DEPLOYED (WP-D, merged `85983c04`, 2026-07-01).** auth.js `tombstoneUsername()` renames the username to `<name>__inactive_<hex>` on deactivate/soft-delete (idempotent, VARCHAR(60)-clamped); migration `0081` backfills already-inactive/legacy names. **Verified live** (Planning preview: create user → deactivate → recreate SAME username → 200, no "taken"). | CLOSED |
| O40 | **Test-fixture junk in the shared (prod) DB — visible to Carter.** 21 of 23 clients are test fixtures (`casc-client-*`/`billing-client-*`/`*-tmr0*`/`client-<tag>-<n>`) left by killed test runs + CEO test suites; only real clients = **DEMO — PSC**, **DEMO — Live Loop**. Plus throwaway test users (pv_verify_admin, wpd_reuse_test*, any dd_*) — **already DEACTIVATED** by Planning (neutralized the known-password admin). **Needs FK-AWARE deletion, not a blind cascade:** clients → ECs → service_areas → jobs (+ some SAs may be billed → `invoices` FK RESTRICT), users → training_progress/user_training_* FKs. Safest = the app DELETE endpoints (handle cascade/guards) or a careful bottom-up script. Deferred from the WP-C/D wave (time). | hygiene (Planning) | focused cleanup pass | OPEN |
