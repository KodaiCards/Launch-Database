# Idea Registry

> Owned by Planning. **Every significant idea / enhancement / suggestion lands here so nothing is ever lost** (core Planning mandate). Lifecycle: `IDEA → DOCUMENTED → promoted (becomes a feature/system) / DEFERRED / REJECTED`. Last updated 2026-06-28.

| # | Idea | Related | Date | Priority | Status | Notes |
|---|---|---|---|---|---|---|
| **I7** | **Demo/dev-access = assemble existing pieces, not a build.** Carter wants demo+dev to access the app before final features go live. | Carter's explicit ask / portals (chunk 10) / chunk 01 PORTAL_MODE | 2026-06-29 | Med-High | DOCUMENTED | **Found in chunk 10.** Pieces already exist: (1) **client-facing demo** → `client_portal_v2` token (admin creates a demo `client_organization`+user+token, shown-once/expiring/revocable/org-branded, `/client/login/:rawToken`) — scoped, no real account, instantly revocable. (2) **staff/admin demo** → impersonation (view-as-staff, 1h, audited) OR a dedicated scoped demo `users` account. (3) **hide unfinished tabs** → a `PORTAL_MODE`/feature-flag (chunk 01 already has `TRAINING_ONLY_LOCKDOWN`). Design = wire these together + a "demo mode" toggle. Surface when Carter resumes the thread. |
| **I8** | **"Future" customer features are PARTLY BUILT — verify + surface, don't re-spec.** `feature_customer_portal` = `customer_portal.js` live (keystone `/service-areas` + invoices + map); `feature_client_progress_view` = `/api/admin/client-progress` live; `feature_client_portal_completion_view` partially derivable from `/service-areas`+projections. | I2 backend-wired sweep / portals (chunk 10) / 3 customer memories | 2026-06-29 | Med | DOCUMENTED | **Found in chunk 10.** These three memory "future/deferred" items already have working backends. Action: verify live reachability (feature_verify_user_facing discipline), then update those memories + decide what's actually missing (likely just UI polish + the completion/projection view). Avoids re-building what exists. |
| **I5** | **Configurable per-(job,client) invoice templates — ALREADY BUILT, surface + verify it.** Owner uploads a sample PDF → Claude vision → mustache HTML template → puppeteer render. (`invoice_template_engine.js` + `routes/invoice_templates.js`.) | D013 / Billing / `reference_invoice_non_rus_formats` / I2 | 2026-06-29 | High | **DOCUMENTED — built, likely under-surfaced** | **Found in chunk 07b.** Directly satisfies the long-standing "non-RUS clients need their own invoice formats" need — and it's *data-driven* (templates are rows generated from a sample PDF), exactly the D013 ethos. Two actions: (1) **surface to Carter** — is it in use / reachable in the UI? (ties I2 backend-wired-but-no-UI). (2) **verify its DATA source** — it imports `invoiceGenerator`, so it likely fills templates with legacy-projects data → would need the same keystone port as O20. A genuine asset hiding in the codebase. |
| **I6** | **The map/projection ENGINE is already built ahead of the map — surface + leverage it.** `projections.js` + `_map_estimate.js` already implement the **contract-allocation engine** (`contract_allocations`: per-mile hourly alloc, `per_mile=remaining_$÷remaining_miles`), **map-derived construction** (`computeEstimate` = map units × CC catalog when `sa.map_plan_id` set), and EC **budget-burn / projected-completion**. | Map (System C) / Projections (B) / `project_map_requirements_spec` / I1 | 2026-06-29 | High | DOCUMENTED | **Found in chunk 07c.** memory had these "deferred until the map lands" — but the projection MATH exists now; only map *rendering* + KMZ-sync are missing. Means: (1) the map feature has a big head start (wiring the map populates `map_plan_id`/units → these engines light up); (2) projections-by-mileage + per-CC-catalog estimates are demoable today via `/api/projections/*`. Tell Carter — reshapes the map roadmap (it's "render + sync onto an existing engine," not "build projections"). |
| **I4** | **Repoint the hardcoded rate fallback at the EXISTING `pricing_entries` config (don't build a new one).** | D013 / Billing (A) / "nothing can break" | 2026-06-29 | High | DOCUMENTED — **reframed (chunk 07c)** | The config rate source **already exists**: `pricing_entries` (job×program×billing_code→rate, w/ `/api/pricing/lookup` + `/gaps` red-dot) + `jobs.default_rate`. The problem is the `COALESCE(billing_rate, CASE 'inspection' 90 / 're' 100 / 'permitting' 90 ELSE 0)` fallback is hand-pasted across **~6 files** (server.js seed, dashboard.js, projects.js, project_detail.js ×5, revenue.js, billing.js) instead of consulting it. One drifted copy = silent money error. So I4 = **consolidate those CASEs onto `pricing_entries`/`jobs.default_rate`** — a low-risk cleanup onto existing config, not a new table. NOTE: invoice_generator.js correctly uses `jobs.default_rate` (no hardcode) — use it as the model. |
| **I3** | **"Did I bill this?" billing-status per project** (billed / amount / ready-to-bill, at a glance). | Job Board / Billing (System A) | 2026-06-28 | High | **APPROVED — to build** | Carter's worst daily pain; not RUS-dependent; **de-risks the bus-factor** (others can see billing state). **Prior art: it existed in old `admin.html`** — Carter: "should've been in the plan." Ties to I2 (stranded-in-legacy). Candidate early standalone win. |
| **I2** | **"Backend wired but no UI / stranded-in-legacy" surfacing sweep.** Catalog functionality that exists/works but isn't reachable from the operations cluster (e.g. user-account mgmt stranded in `admin.html`), then surface it. | Cutover / Job Board | 2026-06-28 | High | DOCUMENTED | Carter "has no idea what's in the background that should be shown." Extend `docs/cutover_inventory.md`. The hidden half of the cutover. CEO runs it after Track A; reports to Planning. Drove the [[feedback_verify_user_facing]] discipline. |
| **I1** | **Scheduling + monthly revenue projection + project prioritization.** When creating a project / viewing active projects, schedule the work, **project monthly revenue**, and prioritize what to work on. | Projections (B) + Job Board | 2026-06-28 | High | DOCUMENTED | **Driver: Carter reports projections monthly to the owners** — make that report a *live output*, not a manual chore, and use it to decide priorities. Every job already shows a revenue projection + a SUM/rollup; this adds a **time dimension** (which $ lands which month) + a priority view. RUS adds budget-constraint wiring as a config layer (D013). |
