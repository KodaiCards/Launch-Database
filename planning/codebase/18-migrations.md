# 18 — Migrations + schema.sql (THE VERIFICATION CHUNK) — ✅ COMPLETE

> Mapped 2026-06-29. Verified the big STRUCTURAL findings against the actual `schema.sql` (5228 ln, generated snapshot) + migration set. **79 migrations** (`0001` … `0079_training_content_access`). Every major O-finding now has a schema-verified VERIFIED/REFUTED status — this hardens the whole map.

## Verification ledger
| Finding | Status | Schema evidence |
|---|---|---|
| **O14** SA-delete data loss | ✅ **CONFIRMED + REFINED** | `service_area_jobs.service_area_id`→service_areas **ON DELETE CASCADE**; same CASCADE for `service_area_routes`, `service_area_materials`; `service_area_job_assignments.service_area_job_id` CASCADE. **BUT** `time_entries.service_area_job_id`→service_area_jobs **ON DELETE SET NULL** (hours rows SURVIVE, orphaned — NOT deleted) + `time_clock_sessions` SET NULL. **AND** `invoices.service_area_id` + `invoice_items.service_area_job_id` have **NO ON DELETE (=RESTRICT)** → a BILLED SA/job **blocks** the delete. Net: deleting an *unbilled* SA destroys its jobs/routes/materials config (no undo) + orphans hours; billed SAs are FK-protected. **Less catastrophic than "wipes hours," still real config-loss + orphaning with no undo** (fix = wire saveUndoBucket, chunk 15 says infra exists). |
| **O23** hours split-brain | ✅ **CONFIRMED** | `time_entries` has BOTH `project_id` (FK→projects **ON DELETE RESTRICT**, idx) AND `service_area_job_id` (FK→service_area_jobs **SET NULL**, idx). **NO XOR/CHECK constraint** forcing exactly-one (both nullable, split is APP-enforced only). Precedent for a fix exists: `budgets` has `budget_scope_exactly_one CHECK`. |
| **permit_documents dual-key** (ch13) | ✅ CONFIRMED | `project_id` (FK→projects **CASCADE**, idx `_project_id`) + `service_area_job_id` (idx `_saj`). Dual-keyed as mapped. |
| **O17** three SA-ish tables | ✅ CONFIRMED | `CREATE TABLE` for `service_areas` (972), `concentrators` (305), `ec_service_areas` (482) all exist. |
| **O18** contracts dual | ✅ CONFIRMED | `contracts` (348) + `construction_contracts` (319) both exist (the naming-collision pair). |
| **O27** map tables | ✅ CONFIRMED | `map_store` (681), `cost_catalog` (363), `construction_contracts` (319) exist. |
| **I4** rate config | ✅ CONFIRMED | `pricing_entries` (746) exists (the config rate source). |
| **capability grants** (ch10) | ✅ CONFIRMED | `user_portal_access` (1668) exists. |
| **rollup_level** (ch17) | ✅ CONFIRMED | `projects`: `is_rollup` (default false), `rollup_level varchar(20)`, `rollup_key text`, `parent_id`. CHECK `projects_rollup_level_key_required` (rollup ⇒ level+key, or `'legacy_orphan'`). varchar not enum → app-enforced 5 levels + a `legacy_orphan` value (for migrated orphans). Partial index `idx_projects_rollup` WHERE is_rollup. |
| **program enum** | ✅ CONFIRMED | `projects_program_check` = {rus,bau,gfr,other} (matches every chunk). |
| **splice** size | ✅ CONFIRMED bigger | **26 `splice_*` tables** (chunk 12 said ~17 — it's 26; an even larger isolated tool). |
| **training** | ✅ CONFIRMED | 5 `training_*` tables (+ user_training_access/overrides). Newest migration `0079_training_content_access` = the chunk-11 gating. |
| **client portal v2** | ✅ CONFIRMED | 3 tables: `client_organizations`/`client_users`/`client_tokens` (the O25 separate identity). |

## Schema-layer reconciliation (chunk 14 + 09 follow-up)
Three apply paths confirmed; **the 79 numbered migrations are canonical going forward:**
1. **`migrations/0001..0079`** (versioned runner `runMigrations`, chunk 14) — the source of truth; auto-runs on deploy (O13). Newest = 0079.
2. **`schema.sql`** (5228 ln, generated by `npm run schema:sync` via pg_dump; CI-validated) — applied by `initSchema` (2-pass + deferred user-FKs, chunk 14) on a FRESH DB only.
3. **`bootstrapV3Schema`** (chunk 14) + **`bootstrapTimeClockSchema`** (chunk 09, runs every boot) = legacy idempotent DDL that OVERLAPS the above — back-compat cruft. Cleanup target post-cutover (the migrations runner makes these redundant; keeping them risks drift).

## Findings
- **All major structural findings VERIFIED** — the map's architectural claims hold against the real schema. Highest-confidence chunk.
- **O14 refined (important):** the data loss is **config + attribution**, not hours-deletion (hours SET NULL survive) — and **billed SAs are RESTRICT-protected.** Still needs an undo bucket for the unbilled-SA config-destroy case. Severity: medium (was feared high). Updated O14.
- **O23 confirmed unconstrained:** no DB guard on the project_id/service_area_job_id split — the cutover should add a CHECK (budgets shows the pattern) once unified. Updated O23.
- **Schema cleanup target:** 3 overlapping apply paths (migrations + schema.sql + bootstrap DDL ×2). Post-cutover, retire bootstrapV3Schema + the per-module bootstraps; migrations are canonical.
- **79 migrations, idempotent, auto-applied** — the migration discipline is solid (versioned, checksummed, CI-validated drift — chunk 14). This is a healthy part of the stack.

## Reapproach-if
- O14 fix: add saveUndoBucket to the keystone SA delete (chunk 03) — infra exists (chunk 15). The RESTRICT FKs already prevent billed-SA deletion; the gap is unbilled-SA config-destroy + hours-orphaning without undo.
- O23 fix: after unifying hours onto one column, add a CHECK (mirror `budget_scope_exactly_one`).
- Chunk 20 (tests): does any test cover the SA-delete cascade or the hours-column split? (the legacy project_tree_delete + hours_bulk_delete tests exist — chunk 09/06; keystone SA-delete may be untested).
- Post-cutover schema hygiene: retire bootstrapV3Schema + module bootstraps; keep migrations as the single path.