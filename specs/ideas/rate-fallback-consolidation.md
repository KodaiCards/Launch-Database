# Rate-fallback consolidation — silent money-error risk (rescued: I4, pre-canon)
The hardcoded rate fallback `COALESCE(billing_rate, CASE inspection 90 / re 100 / permitting 90)` is hand-pasted across ~6 files (server.js seed, dashboard.js, projects.js, project_detail.js, revenue.js, billing.js) instead of consulting the EXISTING config source (`pricing_entries` + `/api/pricing/lookup` + `jobs.default_rate`). One drifted copy = silent money error. Fix = consolidate the CASEs onto pricing_entries/jobs.default_rate (invoice_generator.js already does it right — use as the model). Low-risk cleanup onto existing config. Slot: billing spec (2.7) or cutover (2.3) — money-path, VO-verified.
