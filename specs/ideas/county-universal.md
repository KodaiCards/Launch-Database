# County-universal — the research answer, don't re-derive (rescued: I13/D014)
L-006: everything groups by county first, everywhere. The tool question is ANSWERED (pre-canon research): **US Census TIGER/Line county boundaries (free, public-domain GeoJSON) + point-in-polygon via turf.js on the existing Leaflet stack** = auto-drawn county lines + county autofill from a permit/job location. County-as-data-model can land AHEAD of the map (schema + grouping in views); boundary-draw/autofill rides map delivery. Note: legacy potential_permits already has a county text column (schema.sql) — reuse/extend, don't parallel. Slot: 2.10 design session (call-up *5).
